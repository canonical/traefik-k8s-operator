project: traefik-k8s-operator
path: /home/ubuntu/proj
kill-timeout: 60m
warn-timeout: 1m
backends:
  integration-test-local:
    systems:
    - ubuntu-24.04:
        username: ubuntu
    type: adhoc
    allocate: |
      DISTRO=$(echo "$SPREAD_SYSTEM" | cut -d- -f1)
      SERIES=$(echo "$SPREAD_SYSTEM" | cut -d- -f2)
      VM_NAME="spread-${DISTRO}-${SERIES}-$$-${RANDOM}"
      VM_NAME=$(echo "$VM_NAME" | tr . -)

      DISK="${DISK:-20}"
      CPU="${CPU:-4}"
      MEM="${MEM:-8}"

      CLOUD_CONFIG=$(mktemp)
      cat > "$CLOUD_CONFIG" <<'ENDCLOUD'
      #cloud-config
      ssh_pwauth: true
      users:
        - name: ubuntu
          lock_passwd: false
          sudo: ALL=(ALL) NOPASSWD:ALL
          shell: /bin/bash
      ENDCLOUD

      CLEANUP_VM=true
      cleanup() {
        rm -f "$CLOUD_CONFIG" 2>/dev/null || true
        if [ "$CLEANUP_VM" = true ] && [ -n "${VM_NAME:-}" ]; then
          lxc delete --force "${VM_NAME}" 2>/dev/null || true
        fi
      }
      trap cleanup EXIT

      lxc launch --vm \
        "${DISTRO}:${SERIES}" \
        "${VM_NAME}" \
        --config "user.user-data=$(cat "$CLOUD_CONFIG")" \
        --config "limits.cpu=${CPU}" \
        --config "limits.memory=${MEM}GiB" \
        --device "root,size=${DISK}GiB" >&2

      # Wait for LXD agent to be ready inside the VM
      while ! lxc exec "${VM_NAME}" -- true &>/dev/null; do sleep 0.5; done

      # Wait for cloud-init and snap seeding
      lxc exec "${VM_NAME}" -- cloud-init status --wait >&2
      lxc exec "${VM_NAME}" -- snap wait system seed.loaded >&2

      # Set ubuntu user password (using lxc exec to avoid YAML escaping issues)
      lxc exec "${VM_NAME}" -- bash -c "echo ubuntu:${SPREAD_PASSWORD} | chpasswd"

      # Enable SSH password authentication
      lxc exec "${VM_NAME}" -- bash -c \
        'if [ -d /etc/ssh/sshd_config.d ]; then
           printf "PasswordAuthentication yes\n" \
             > /etc/ssh/sshd_config.d/00-spread.conf
         fi'
      lxc exec "${VM_NAME}" -- sed -i \
        's/^\s*#\?\s*PasswordAuthentication\>.*/PasswordAuthentication yes/' \
        /etc/ssh/sshd_config
      lxc exec "${VM_NAME}" -- killall -HUP sshd 2>/dev/null || true

      # Get and report the VM's IPv4 address
      while true; do
        RAW_ADDR=$(lxc ls --format csv --columns 4 "name=${VM_NAME}" | head -1)
        ADDR=$(echo "$RAW_ADDR" | awk '{print $1}')
        if [ -n "$ADDR" ]; then
          CLEANUP_VM=false
          ADDRESS "$ADDR"
          break
        fi
        sleep 0.5
      done
    discard: |
      instance_name=$(lxc ls --format json   | jq -r --arg a "$SPREAD_SYSTEM_ADDRESS"     '.[] | select(any(
            .state.network[]?.addresses[]?; .address == $a
          )) | .name' | head -1)
      if [ -n "$instance_name" ]; then
        lxc delete --force "$instance_name"
      fi
    environment:
      SUDO_USER: ubuntu
    prepare: |
      loginctl enable-linger ubuntu
      snap install astral-uv --classic
      export UV_TOOL_BIN_DIR=/usr/local/bin
      export UV_TOOL_DIR=/usr/local/share/uv-tools
      if grep -q 'name = "opcli"' "${SPREAD_PATH}/pyproject.toml" 2>/dev/null; then
        uv tool install "${SPREAD_PATH}" --quiet
      else
        uv tool install       "git+https://github.com/canonical/charm-ci@${OPCLI_GIT_REF:-main}"       --quiet
      fi
      opcli install spread
      opcli install tox
      opcli install concierge
      opcli env provision -c "$CONCIERGE"
      usermod -aG lxd ubuntu || true
      runuser -l ubuntu -c   "cd \"${SPREAD_PATH}\" && opcli env deploy-registry"
      opcli artifacts push-images
      chown -R ubuntu:ubuntu "${SPREAD_PATH}"
environment:
  CONCIERGE: '$(HOST: echo "${CONCIERGE:-concierge.yaml}")'
  OPCLI_GIT_REF: '$(HOST: echo "${OPCLI_GIT_REF:-main}")'
exclude:
- .git
- .tox
- .venv
- .*_cache
suites:
  tests/integration/:
    summary: integration tests
    backends:
    - integration-test-local
    environment:
      MODULE/test_basic_auth: test_basic_auth
      MODULE/test_charm_health: test_charm_health
      MODULE/test_charm_ipa: test_charm_ipa
      MODULE/test_charm_ipu: test_charm_ipu
      MODULE/test_charm_route: test_charm_route
      MODULE/test_charm_tcp: test_charm_tcp
      MODULE/test_forward_auth: test_forward_auth
      MODULE/test_pebble_restart_after_cert_relation_joined: 
        test_pebble_restart_after_cert_relation_joined
      MODULE/test_tcp_ipa_compatibility: test_tcp_ipa_compatibility
      MODULE/test_tcp_ipu_compatibility: test_tcp_ipu_compatibility
      MODULE/test_tls: test_tls
      MODULE/test_tls_multi_unit_jubilant: test_tls_multi_unit_jubilant
      MODULE/test_upstream_ingress: test_upstream_ingress
      MODULE/test_workload_tracing: test_workload_tracing
reroot: ..
