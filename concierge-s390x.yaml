juju:
  channel: 3.6/stable
  model-defaults:
    test-mode: "true"
    automatically-retry-hooks: "true"
  extra-bootstrap-args: --config agent-stream=proposed

providers:
  lxd:
    enable: true

host:
  snaps:
    charmcraft:
