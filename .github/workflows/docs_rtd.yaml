name: RTD workflows

on:
  pull_request:

jobs:
  rtd-docs-checks:
    uses: canonical/operator-workflows/.github/workflows/docs_rtd.yaml@c9e78575cf199e07860da02d31d391dbccc68bf0  # main
    secrets: inherit
    with:
      enable-sphinx-python-dependency-build-checks: false
      python-version: '3.11'

