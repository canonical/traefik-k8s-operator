name: RTD workflows

on:
  pull_request:

jobs:
  rtd-docs-checks:
    uses: canonical/operator-workflows/.github/workflows/docs_rtd.yaml@swetha1654-patch-1
    secrets: inherit
    with:
      enable-sphinx-python-dependency-build-checks: false
      python-version: '3.11'

