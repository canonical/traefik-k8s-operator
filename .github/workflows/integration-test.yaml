name: Integration Tests

on:
  pull_request:
    branches:
      - main

jobs:
  integration-test:
    permissions:
      actions: read
      contents: read
      packages: write
    uses: canonical/charm-ci/.github/workflows/integration-test.yml@main
    secrets: inherit
    with:
      working-directory: .
