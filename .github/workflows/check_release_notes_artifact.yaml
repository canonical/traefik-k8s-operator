name: 'Check for release notes artifact'

on:
  pull_request:

jobs:
  check-change-artifacts:
    permissions:
      contents: write
    uses: canonical/release-notes-automation/.github/workflows/pr-compliance.yml@chore/update-pr-compliance-workflow
    secrets: inherit
    with:
      change-artifact-dir: docs/release-notes/artifacts
