# Copyright 2022 Canonical Ltd.
# See LICENSE file for licensing details.
name: tcp-requirer-mock
display-name: tcp-requirer-mock
description: tcp tester
summary: tcp tester

requires:
  ingress-per-unit:
    interface: ingress-per-unit
    limit: 1

containers:
  tcp-server:
    resource: tcp-server-image

resources:
  tcp-server-image:
    type: oci-image
    description: OCI image for tcp-server
    upstream-source: python:slim-buster

