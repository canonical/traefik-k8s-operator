# Copyright 2022 Canonical Ltd.
# See LICENSE file for licensing details.

type: charm

platforms:
  ubuntu@20.04:amd64:
  ubuntu@26.04:amd64:

parts:
  charm:
    source: .
    plugin: uv
    build-packages: [git, libffi-dev]
    build-snaps: [astral-uv]
    override-build: |
      craftctl default
      git describe --always > $CRAFT_PART_INSTALL/version

